<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.graduate.framework.mapper.WarehouseMapper">
    <resultMap id="WarehouseResult" type="com.graduate.framework.entity.Warehouse">
        <id property="id" column="id"/>
        <result property="traceCode" column="traceCode"/>
        <result property="amount" column="amount"/>
        <result property="sendDate" column="sendDate"/>
        <result property="origin" column="origin"/>
        <result property="name" column="name"/>
        <result property="recvDate" column="recvDate"/>
        <result property="productId" column="productId"/>
    </resultMap>

    <select id="findAll" resultMap="WarehouseResult">
        SELECT * FROM warehouse
    </select>

    <select id="findById" resultMap="WarehouseResult">
        SELECT * FROM warehouse WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.graduate.framework.entity.Warehouse">
        INSERT INTO warehouse (id, traceCode, amount, sendDate, origin, name, recvDate, productId)
        VALUES (#{id}, #{traceCode}, #{amount}, #{sendDate}, #{origin}, #{name}, #{recvDate}, #{productId})
    </insert>

    <update id="update" parameterType="com.graduate.framework.entity.Warehouse">
        UPDATE warehouse SET
            traceCode = #{traceCode},
            amount = #{amount},
            sendDate = #{sendDate},
            origin = #{origin},
            name = #{name},
            recvDate = #{recvDate},
            productId = #{productId}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM warehouse WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM warehouse WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="findByProductName" resultMap="WarehouseResult">
        SELECT * FROM warehouse WHERE name = #{name}
    </select>
</mapper>
